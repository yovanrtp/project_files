apiVersion: v1
kind: Service
metadata:
  name: flask-app-service
spec:
  type: NodePort
  selector:
    app: flask-app
  ports:
    - protocol: TCP
      port: 5000        # Cluster port
      targetPort: 5000  # Container port
      nodePort: 30080   # External port (optional, can be auto-assigned)
